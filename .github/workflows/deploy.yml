name: Deploy Gateway and ADK Agents

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  deploy-gateway:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Needed for Google Cloud Workload Identity Federation
    steps:
      - uses: actions/checkout@v4

      # --- UNCOMMENT BELOW TO DEPLOY GATEWAY TO GOOGLE CLOUD RUN VIA SOURCE ---
      # - name: Authenticate to Google Cloud
      #   uses: google-github-actions/auth@v2
      #   with:
      #     workload_identity_provider: '${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}'
      #     service_account: '${{ secrets.GCP_SERVICE_ACCOUNT }}'
      #
      # - name: Deploy Gateway to Cloud Run
      #   uses: google-github-actions/deploy-cloudrun@v2
      #   with:
      #     service: gateway-service
      #     source: .
      #     region: us-central1
      #     env_vars: |
      #       APP_NAME=gateway

  deploy-agents:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Needed for Google Cloud Workload Identity Federation
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          
      - run: npm ci
      
      # Build the monorepo workspace to compile the TS to JS
      - run: npm run build

      # --- UNCOMMENT BELOW TO ACTUALLY DEPLOY AGENTS TO GOOGLE CLOUD VIA ADK ---
      # - name: Authenticate to Google Cloud
      #   uses: google-github-actions/auth@v2
      #   with:
      #     workload_identity_provider: '${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}'
      #     service_account: '${{ secrets.GCP_SERVICE_ACCOUNT }}'
      #
      # - name: Deploy Context Builder Agent via ADK
      #   run: npx adk deploy --project dist/apps/agent-context-builder
      #
      # - name: Deploy Code Reviewer Agent via ADK
      #   run: npx adk deploy --project dist/apps/agent-code-reviewer
